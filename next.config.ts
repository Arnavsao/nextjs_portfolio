import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ['images.unsplash.com', 'aceternity.com', 'drive.com'], // Added aceternity.com
  },
};

export default nextConfig;